This is a TUIO cursor message handler, that serves for tracking people walking on a room, and preserving their identities when their blobs merge. It reads at port 3333 and sends new messages to a new destination.
